

public class Circle {
        public static double CalcArea(double r)
        {
            return Math.PI * r * r;
        }
        public static double calcCircum(double r)
        {
            return 2 * Math.PI * r;
        }
}
